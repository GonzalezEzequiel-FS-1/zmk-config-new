# -------------------------------------------------
#  YOUR ORIGINAL SETTINGS (keep them!)
# -------------------------------------------------
CONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=3000
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y
#CONFIG_ZMK_DISPLAY=n
#CONFIG_ZMK_RGB_UNDERGLOW=y
#CONFIG_WS2812_STRIP=y

# -------------------------------------------------
#  WIRED-ONLY (USB ONLY, NO BLUETOOTH)
# -------------------------------------------------
CONFIG_ZMK_BLE=n          # <-- disables BLE radio, advertising, pairing
CONFIG_ZMK_USB=y          # <-- USB HID (default, but explicit is safe)
CONFIG_USB_DEVICE_STACK=y # <-- required for USB

# Optional: silence battery reporting (no BT → no battery)
#CONFIG_ZMK_BATTERY_REPORT=n